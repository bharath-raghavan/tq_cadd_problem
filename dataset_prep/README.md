# Dataset Preparation

Compounds that are purchasable and lead-like are downloaded from the ZINC20 database (https://zinc20.docking.org). Through various similarity-based criteria, this is reduced to a list of 250 compounds for docking.

The Jupyter Notebook, named `run.ipynb`, details the filtering procedure.